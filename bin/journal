#!/bin/bash

if [[ $# < 1 ]]
then
  echo "usage: journal 'message'"
  exit 1
fi

if [[ "$JOURNAL_ROOT" == "" ]]
then
    JOURNAL_ROOT=$HOME/Dropbox/journal
fi

journal=$(date +"$JOURNAL_ROOT/%Y/%m/%d.txt")

echo "$1" > "$journal"
